diff '--color=auto' -p -X ../chromium-loongarch64/chromium/exclude -N -u -r a/build/rust/gni_impl/rust_target.gni b/build/rust/gni_impl/rust_target.gni
--- a/build/rust/gni_impl/rust_target.gni	2000-01-01 00:00:00.000000000 +0800
+++ b/build/rust/gni_impl/rust_target.gni	2000-01-01 00:00:00.000000000 +0800
@@ -501,6 +501,9 @@ template("rust_target") {
       if (_rustc_metadata != "") {
         rustflags += [ "-Cmetadata=${_rustc_metadata}" ]
       }
+      if (current_cpu == "loong64") {
+        rustflags += [ "-Ccode-model=medium" ]
+      }
       rustenv = _rustenv
 
       sources = _rust_sources
