diff '--color=auto' -p -X chromium-loongarch64/exclude -N -u -r a/build/config/compiler/BUILD.gn b/build/config/compiler/BUILD.gn
--- a/build/config/compiler/BUILD.gn	2024-01-10 08:13:24.074601700 +0800
+++ b/build/config/compiler/BUILD.gn	2024-01-14 15:01:25.630010525 +0800
@@ -621,10 +621,10 @@ config("compiler") {
     # TODO(crbug.com/1491036): This isn't supported by Cronet's mainline llvm version.
     if (default_toolchain != "//build/toolchain/cros:target" &&
         !llvm_android_mainline) {
-      cflags += [
-        "-mllvm",
-        "-split-threshold-for-reg-with-hint=0",
-      ]
+      # cflags += [
+      #   "-mllvm",
+      #   "-split-threshold-for-reg-with-hint=0",
+      # ]
       if (use_thin_lto && is_a_target_toolchain) {
         if (is_win) {
           ldflags += [ "-mllvm:-split-threshold-for-reg-with-hint=0" ]
